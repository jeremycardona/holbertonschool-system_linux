#include "hreadelf.h"

char *g_st(elf_t *elf_header, size_t i)
{
    // Function to get symbol type
}

char *g_sb(elf_t *elf_header, size_t i)
{
    // Function to get symbol binding
}

char *g_sv(elf_t *elf_header, size_t i)
{
    // Function to get symbol visibility
}